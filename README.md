<img width="1190" height="447" alt="image" src="https://github.com/user-attachments/assets/508d3e78-c738-43ee-93ee-9b3cebb66fd7" /># Claude TUI

Удобное графическое приложение для работы с Claude AI через голосовой ввод на русском языке.

## Что умеет приложение

- **Голосовой ввод на русском** — говорите по-русски, приложение распознает речь
- **Автоматический перевод** — русская речь автоматически переводится на английский для Claude
- **Несколько вкладок** — работайте с несколькими сессиями Claude одновременно
- **История чата** — скроллинг вверх/вниз для просмотра всей истории разговора
- **Тёмная тема** — приятный интерфейс для работы

## Как пользоваться

### Запуск

```bash
python main.py
```

### Голосовой ввод

1. Нажмите кнопку **🎤** в нижней панели (или дождитесь загрузки Claude)
2. Говорите по-русски
3. Нажмите кнопку **🎤** ещё раз чтобы остановить запись
4. Приложение автоматически:
   - Распознает вашу речь (GigaAM)
   - Переведёт на английский (MarianMT)
   - Отправит запрос в Claude

### Горячие клавиши

| Клавиши | Действие |
|---------|----------|
| `Ctrl+T` | Новая вкладка |
| `Ctrl+W` | Закрыть вкладку |
| `Ctrl+Shift+C` | Копировать выделенный текст |
| `Ctrl+Shift+V` | Вставить текст |
| `Ctrl+Shift+Q` | Выход |

### Навигация

- **Колёсико мыши** — прокрутка истории вверх/вниз
- **Клик по вкладке** — переключение между сессиями

## Установка

### Требования

- Python 3.10 или выше
- Claude CLI (`npm install -g @anthropic-ai/claude-code`)

### Шаги установки

```bash
# 1. Клонировать или скачать проект
cd claude-tui

# 2. Создать виртуальное окружение
python -m venv .venv

# 3. Активировать окружение
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# 4. Установить зависимости
pip install -r requirements.txt

# 5. Установить GigaAM (распознавание речи)
pip install -e GigaAM/

# 6. Запустить
python main.py
```

## Технологии

| Компонент | Технология |
|-----------|------------|
| Интерфейс | PyQt6 |
| Распознавание речи | GigaAM (Sber) |
| Перевод | MarianMT (Helsinki-NLP) |
| Терминал | WinPTY + Pyte |

## Структура проекта

```
claude-tui/
├── main.py          # Точка входа
├── app.py           # Инициализация приложения
├── window.py        # Главное окно
├── terminal.py      # Эмулятор терминала
├── widgets.py       # UI компоненты (заголовок, статус-бар)
├── voice.py         # Голосовой ввод (GigaAM)
├── translator.py    # Перевод ru→en (MarianMT)
├── theme.py         # Цветовая схема
├── GigaAM/          # Модель распознавания речи
└── requirements.txt # Зависимости
```

## Решение проблем

### Не работает голосовой ввод
- Убедитесь что микрофон подключен и работает
- Проверьте что GigaAM установлен: `pip install -e GigaAM/`

### Долго загружается при первом запуске
- При первом использовании загружаются модели (~500MB для GigaAM, ~300MB для переводчика)
- Последующие запуски будут быстрее

### Ошибка "claude not found"
- Установите Claude CLI: `npm install -g @anthropic-ai/claude-code`
- Перезапустите терминал

## Лицензия

MIT
